@model EnumerableWithTitle<Movie>

@if (Model.Any())
{
	<h2>@Model.Title</h2>
	<ul>
		@foreach (var movie in Model)
		{
			<li>
				<a asp-action="Details" asp-controller="Movie" asp-route-id="@movie.MovieId">@movie.Title</a>
			</li>
		}
	</ul>
}
